# TapNow Frontend Environment Variables

# Backend API Base URL (leave empty if using Vite dev proxy in development, or specify full domain for production)
VITE_API_BASE_URL=

# Default Operator API Key for X-API-Key header (can also be configured in UI / LocalStorage)
VITE_API_KEY=your-secure-api-key-here
